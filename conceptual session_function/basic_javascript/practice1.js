/**
 * . এমন  একটা  প্রোগ্রাম  লিখো   যেখানে  একটা  নম্বর  ইনপুট  করলে  কনসোল এ বলে  দিবে  যে  এইটা  জোর  নাকি  বিজোড় 

 */
   function even(num){
      var number=[];
      for(var i=0;i<=num.length;i++){
        var numbers=number[i];
        if(numbers%2==0){
            number.push();
        }
      }
      return number;
   }

  var number=[23,32,34,43,45,54,46];
 var output =even(number);
 console.log(output);
 //hosse na

 